config APP_TEST10
    bool "AADL Test 10: active -> active threads using scalar send w/no dispatcher"
    default n
    select APP_CAMKES_EXECUTIVE
    help
        AADL Test 10: active -> active threads using scalar send w/no dispatcher. 
