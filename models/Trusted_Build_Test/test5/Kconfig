config APP_TEST5
    bool "AADL Test 5: active -> active threads using struct send"
    default n
    select APP_CAMKES_EXECUTIVE
    help
        AADL Test 5: active -> active threads using struct send. 
