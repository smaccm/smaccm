config APP_TEST11
    bool "AADL Test 11: active -> active threads using struct send w/no dispatcher"
    default n
    select APP_CAMKES_EXECUTIVE
    help
        AADL Test 11: active -> active threads using struct send w/no dispatcher. 
