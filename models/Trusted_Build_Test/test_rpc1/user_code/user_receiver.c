#include <smaccm_receiver.h>
#include <receiver.h>

void add_subtract_add_two_numbers(uint32_t A, uint32_t B, uint32_t *result) {
	*result = A + B;
}

void add_subtract_subtract_two_numbers(uint32_t A, uint32_t B, uint32_t *result) {
	*result = A - B;
}
